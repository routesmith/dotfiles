# Never commit Claude/AI session files
.claude/
CLAUDE.md
**/.claude/**
**/CLAUDE.md

# Personal — local-only redaction rules + their fixtures (the values they hide
# must never be tracked) and the leak-guard deny list.
dot_zsh/exports.local
dot_zsh/exports.local.tmpl
dot_config/paste-rs/sanitize.sed
dot_config/paste-rs/sanitize.test
dot_config/leak-guard/patterns.local
dot_config/tools-local/private_hermes-fleet-targets.local.json

# git credential store — plaintext tokens, never track even if copied into source
.git-credentials
dot_git-credentials
